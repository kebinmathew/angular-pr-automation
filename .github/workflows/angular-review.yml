name: Angular PR Review

on:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm install

      # Install reviewdog correctly
      - uses: reviewdog/action-setup@v1

      # Run ESLint
      - name: Run ESLint
        run: |
          npx eslint "src/**/*.{ts,html}" \
          -f checkstyle \
          -o eslint-result.xml || true

      # Create empty file if no errors
      - name: Ensure result file
        run: |
          if [ ! -f eslint-result.xml ]; then
            echo '<?xml version="1.0"?><checkstyle></checkstyle>' > eslint-result.xml
          fi

      # Correct reviewdog command
      - name: Reviewdog
        run: |
          reviewdog -f=checkstyle \
            -name="angular-review" \
            -reporter=github-pr-review \
            -level=warning \
            < eslint-result.xml
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
